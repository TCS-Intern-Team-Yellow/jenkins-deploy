pipeline {
	agent any
	stages {
		stage('Sample') {
			steps {
				sshagent (credentials: ['TCS_YELLOW_TEAM_PEM']) {
					sh 'ssh -o StrictHostKeyChecking=no -l yellow 10.0.143.117 uname -a'
				}
			}
		}
	}
}