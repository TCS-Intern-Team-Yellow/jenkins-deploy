pipeline {
	agent any
	stages {
		stage('Sample') {
			environment {
				PEM_KEY = credentials('TCS_YELLOW_TEAM_PEM')
            }
			steps {
				sh 'ssh -tty -i $PEM_KEY ec2-user@10.0.156.103'
			}
		}
	}
}